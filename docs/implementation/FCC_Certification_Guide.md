# FCC 认证出口指南 (Export to USA Compliance)

针对 AI 语音公仔出口美国市场，FCC ID 是强制性准入门槛。本指南基于“降低成本、缩短周期”的策略制定。

---

## 1. 认证策略：模组复用 (Module Inheritance)

我们选用的 **ESP32-S3-WROOM-1** 模组，乐鑫原厂已经完成了 FCC 单模块认证。

- **核心策略**：**利用原厂 ID，只做差异化测试**。
- **原厂 FCC ID**：`2AC7Z-ESPS3WROOM1` (请以模组屏蔽罩实际丝印为准)。
- **操作方式**：
  1. 在你的产品后盖标签上，必须标注：`Contains FCC ID: 2AC7Z-ESPS3WROOM1`。
  2. 向实验室申请做 **FCC Part 15C (RSE Only)** 测试，验证装入外壳后的辐射杂散是否超标。

---

## 2. 费用与周期预估 (深圳实验室参考价)

| 项目 | 自主设计射频 (Discrete) | **采用认证模组 (Module)** | 备注 |
| :--- | :--- | :--- | :--- |
| **测试项目** | 传导、辐射、带宽、跳频等全套 | **仅辐射杂散 (RSE)** | 模组已验证过射频源头 |
| **测试费用** | ¥35,000 - ¥50,000 | **¥6,000 - ¥9,000** | **立省 70%** |
| **认证周期** | 4 - 6 周 | **2 - 2.5 周** | 极大缩短上市时间 |
| **证书类型** | 全新 FCC ID | 复用 ID 或 C2PC | 标签需标注 "Contains..." |

> **🚨 决策红色警报 (Critical Decision)**
> *   **算账对比**：500 台模组比芯片多花约 **¥2,500** (BOM 成本)，但 FCC 认证费少花 **¥25,000+**。
> *   **结论**：**对于首批 < 10,000 台的量，严禁自己做板载射频！必须买乐鑫模组！**

---

## 3. 研发阶段的 FCC 合规设计红线 (Design for Compliance)

为了避免在实验室出现 "RSE Fail" 导致数万元的整改费，请务必在设计阶段通过以下审查：

### 3.1 硬件设计红线 (Hardware Checklist)
- [ ] **模组供电去耦**：ESP32 模组的 3.3V 供电引脚必须紧贴放置 **10uF + 0.1uF** 陶瓷电容。防止电源线成为辐射发射源。
- [ ] **天线禁区**：模组 PCB 天线投影区域及向外延伸 **5mm** 范围内，主板和底板严禁铺铜、走线或放置金属件（如螺丝、电池）。
- [ ] **USB 辐射抑制**：在 USB D+/D- 差分线上预留 **共模电感 (Common Mode Choke)** 焊盘（如 90Ω @ 100MHz）。若测试超标，补焊该电感通常能“救命”。
- [ ] **晶振接地**：所有的晶振外壳必须良好接地，晶振下方严禁走其他信号线。

### 3.2 固件设计红线 (Firmware Checklist)
- [ ] **国家码锁定 (Country Code)**：必须在软件初始化时调用 `esp_wifi_set_country()` 将 `cc` 设为 `"US"`。**严禁开启 Channel 12-14**，否则在美国属违法频段，直接拒签。
- [ ] **发射功率限制**：不要盲目追求穿墙王。建议调用 `esp_wifi_set_max_tx_power()` 将最大功率限制在 **78 (19.5dBm)** 以下，避免功率过大导致“带外杂散”超标。
- [ ] **用户不可进入的测试模式**：FCC 要求提供的“定频测试固件”必须是普通用户无法触发的（例如需要物理拆机接串口才能进），以防用户误开干扰基站。

---

## 4. 必须准备的“定频固件” (Test Firmware)

实验室需要你提供一种特殊的固件，这也是大多数初创团队最容易卡壳的地方。

**要求：**
- 公仔必须能通过串口指令，强制让 Wi-Fi **持续发射 (Continuous TX)**。
- 必须能调节：信道 (CH1-11)、模式 (802.11b/g/n)、发射功率。
- **解决方案**：直接烧录乐鑫官方提供的 **RF Test Bin** (ESP-IDF 中有现成工具)，无需自己写代码。

---

## 4. 标签规范 (Labeling Requirement)

FCC 对产品标签有严格要求，必须包含以下信息：
1. **Model Name**: 产品型号。
2. **FCC ID 声明**: `Contains FCC ID: XXXXX-XXXXX`。
3. **警示语 (Part 15.19)**: "This device complies with Part 15 of the FCC Rules..." (如产品太小，可印在说明书上)。
4. **原产地**: `Made in China`。

---

## 5. 电池与运输认证 (UN38.3)
除了 FCC，出口美国走空运/海运，锂电池必须有 **UN38.3 测试报告** 和 **MSDS**。
- **策略**：**不要自己做**。直接采购已经有 UN38.3 证书的电芯（如向电池厂索要），否则自己做一次要 ¥5,000+ 且耗时 1 个月。
